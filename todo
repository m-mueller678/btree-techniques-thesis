-add comments
-check merge
-header
-test.cpp with payloads
-duplicates
-btreenode -> btreepage?

optimizations:
-slot is enough short cut
-prefix comparison: always bcmp first?
-long2: dup root node -> shorter separators

-hint
-trie
-manual memcmp => check if in cache line or copy key
